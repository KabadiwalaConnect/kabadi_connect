{
  "name": "kabadi-collector-rule-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test rules.test.mjs",
    "verify": "firebase emulators:exec --only firestore --project demo-kabadi-collector --config ../firebase.test.json \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "13.35.1"
  }
}
